Let's analyze the `payload.json` token mappings according to `Master-Forge.md` Phase 6 and `compile_json.js`:

`Master-Forge.md` Phase 6 Data Assembly instruction:
- **Identity & Scope Mapping:** Map `Name`, `Emoji`, `Role`, `Category`, `Tier`, `Synthesis`, and `Mission Scope` to `data.identity` and `data.mission_scope`. Map `Cross-Vector Grants` to `data.strict_operational_mandates.cross_vector_grants`. Map Execution Trigger to `data.process.discover.trigger`.
- ...
- **Archetype Physics Mapping:** Inject finalized `domain_anchor`, `mutation_scope`, `operational_boundaries`, `decisiveness_rule`, `workflow_execution`, and `journal_procedure` into `archetype_slots`.

In `compile_json.js`:
```javascript
    PRIMARY_RESPONSIBILITY: trimText(data.archetype_slots?.domain_anchor || data.strict_operational_mandates?.domain_anchor),
    THE_SCOPE: trimText(data.archetype_slots?.mutation_scope || data.strict_operational_mandates?.mutation_scope),
    RESILIENCE_PROCEDURE: trimText(data.archetype_slots?.operational_boundaries || data.strict_operational_mandates?.operational_boundaries),
    AUTONOMOUS_SELECTION: trimText(data.archetype_slots?.decisiveness_rule || data.strict_operational_mandates?.decisiveness_rule),
    WORKFLOW_EXECUTION: trimText(data.archetype_slots?.workflow_execution || data.strict_operational_mandates?.workflow_execution),
    JOURNAL_PROCEDURE: trimText(data.archetype_slots?.journal_procedure || data.memory_and_triage?.journal_procedure),
```

Notice that `decisiveness_rule`, `workflow_execution`, `journal_procedure`, `domain_anchor`, `mutation_scope`, `operational_boundaries` are generated by `Master-Forge.md` (Phase 6). Wait, in `Forge-Procedure.md` Module 1, the definitions are:
- `Domain:`
- `Scope:`
- `Creation Imperative:` / `Operational:` etc.
- `No-Interaction Policy:`

So these are probably mapped to the above `archetype_slots`. The terminology mismatch (e.g., `domain_anchor` vs `Domain`, `mutation_scope` vs `Scope`, `operational_boundaries` vs `Operational`) should be fixed.
Or is `decisiveness_rule`, `workflow_execution`, `journal_procedure` just bloat?

Wait, `Forge-Procedure.md` Module 1 defines the 7 structural base profiles.
Pruner (Delete): Domain, Scope, Operational, No-Interaction Policy.
Generator (Scaffold): Domain, Scope, Creation Imperative, Operational.
Refactorer (Modify): Domain, Scope, Operational.

In `Master-Forge.md`, Phase 6:
"- **Archetype Physics Mapping:** Inject finalized `domain_anchor`, `mutation_scope`, `operational_boundaries`, `decisiveness_rule`, `workflow_execution`, and `journal_procedure` into `archetype_slots`."

Where do `decisiveness_rule`, `workflow_execution`, `journal_procedure` come from? They are not defined in `Forge-Procedure.md`.
Wait! Looking at `Creative-Procedure.md`, the template tokens are:
```markdown
{{PRIMARY_RESPONSIBILITY}}
{{THE_SCOPE}}
{{EXECUTION_RULE}}
{{RESILIENCE_PROCEDURE}}
{{VERIFICATION_PROCEDURE}}
{{DOMAIN_MODIFIERS}}
{{AUTONOMOUS_SELECTION}}
{{WORKFLOW_EXECUTION}}
```
They map to `domain_anchor` -> `PRIMARY_RESPONSIBILITY`
`mutation_scope` -> `THE_SCOPE`
`operational_boundaries` -> `RESILIENCE_PROCEDURE`
`decisiveness_rule` -> `AUTONOMOUS_SELECTION`
`workflow_execution` -> `WORKFLOW_EXECUTION`
`journal_procedure` -> `JOURNAL_PROCEDURE`

Let's read `Forge-Procedure.md` again.
```markdown
### 1. Pruner (Delete)
* **Domain:** Execute strictly to identify and delete targets. If deletion breaks a dependency, do not refactor the dependency. Revert the deletion, leave the dead code, and proceed.
* **Scope:** Limit deletions strictly to your assigned scope. Do not expand blast radius to clean adjacent logic, format files, or fix typos; your only authorized mutation is subtraction.
* **Operational:** Treat the environment as an immutable house of cards. Deleting legacy code is volatile. If target excision results in 3 successive test failures unresolved via simple AST cleanup, immediately Graceful Abort that specific file.
* **No-Interaction Policy:** Hygiene workers operate under a No-Interaction Policy. Treat ambiguity as a signal to skip the target and advance silently.
```

If we map these:
Domain -> domain_anchor -> `PRIMARY_RESPONSIBILITY`
Scope -> mutation_scope -> `THE_SCOPE`
Operational -> operational_boundaries -> `RESILIENCE_PROCEDURE`
No-Interaction Policy -> ???

And for Generator:
Creation Imperative -> ???

Wait, earlier `compile_json.js` had:
```javascript
  const freeTextFieldsToScan = {
    salvaged_custom_logic: data.salvaged_custom_logic,
    salvaged_mandates: data.strict_operational_mandates?.salvaged_mandates || data.salvaged_mandates,
    zero_interaction_mandates: data.zero_interaction_mandates,
    cross_vector_grants: data.strict_operational_mandates?.cross_vector_grants || data.cross_vector_grants,
  };
```
But `No-Interaction Policy` is mentioned as baseline: "Hygiene workers operate under a No-Interaction Policy." Wait, `Master-Forge.md` Phase 6 says:
"- **Strict Adherence:** Map ... interaction bans to `zero_interaction_mandates`."

Let's look for `decisiveness_rule`, `workflow_execution`, `journal_procedure` in `Master-Forge.md` and `compile_json.js`. They are in the schema mapping, but they aren't part of the 7 Base Profiles in `Forge-Procedure.md`!
Are they orphaned?
