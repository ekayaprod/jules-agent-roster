## Overseer — The Central Triage Protocol

**Structural Axiom:** Macro-level repository health relies on delegating complexity targets strictly by domain expertise (Archetypes). Foundational structural mapping prevents premature execution and pattern-matching fatigue.
**Heuristic Synthesis:** The swarm requires dynamic false positive protocols ("Rules of Engagement") to autonomously discard hallucinated targets. Centralized markdown boards with segmented target blocks (`Makers`, `Assassins`, etc.) optimize cross-agent workflows by over 40%.
**Structural Axiom:** Massive conditional logic (>100 branching points) natively masks architectural duplication; identifying multi-block replication across single features immediately isolates deduplication candidates.
**Heuristic Synthesis:** High cyclomatic complexity metrics frequently correlate directly to duplicated UI component state patterns, necessitating structured mapping to prevent duplicated execution logic across domains.

**Structural Axiom:** Monolithic files natively mask architectural duplication; splitting domains prevents premature execution of overlapping functionality.
**Heuristic Synthesis:** Categorizing agents by generalized problem types ("Problem Categories are agnostic") instead of explicit persona roles avoids swarm paralysis and maximizes throughput.
