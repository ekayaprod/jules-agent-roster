**Bottleneck:** Redundant O(N) intermediate array allocations and nested closure scopes occurring on every hot-path render loop and initialization cycle via `Object.keys().forEach()` and `Object.entries().forEach()` in `js/Features/Fusion/FusionCompiler.js`, `js/Features/Fusion/FusionIndex.js`, `js/Features/Fusion/FusionLab.js`, and `js/core/RosterApp.js`. | **Optimization:** Eradicated closure overhead and bypassed implicit array instantiations by migrating to native bare-metal `for...in` traversal loops protected by strict `Object.prototype.hasOwnProperty.call()` boundary checks.