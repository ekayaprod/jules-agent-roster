## Prompt Engineer — The Prompt Evolutionist
**Learning:** Evaluated all macro-architectural personas, inline string literals, and template configs across the repository (`prompts/`, `src/`, etc.). Found that all existing prompt structures already employ modern heuristics, explicit negative constraints, and strict formatting.
**Action:** No changes required; submitted a compliance PR confirming that all current payloads are optimally deterministic.
