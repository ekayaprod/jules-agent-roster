* **Regression:** A structural update introduced a separate `Clusterize.js` search view and isolated the `PinnedManager`, breaking the UI synchronization logic when agents are pinned from active search/fusion contexts. | **Recovery:** Patched `RosterApp.js` to dynamically re-synchronize all `.pin-btn` DOM instances and enforced strict string coercion in `PinnedManager.js`.
* **Regression:** Added playwright/ directory for UI testing, which jest-runtime attempted to parse during `test:unit`, triggering a fatal `Cannot use import statement outside a module` error. | **Recovery:** Configured `testPathIgnorePatterns` in `package.json` to explicitly exclude the `/playwright/` path, restoring compilation and achieving a clean test pass.
* **Regression:** A structural update added Playwright tests which Jest attempts to execute and fails on ECMAScript Modules syntax. | **Recovery:** Reconciled Jest configuration by appending `/node_modules/` and `/playwright/` to `testPathIgnorePatterns` in `package.json`, successfully restoring the test suite.
* **Regression:** A structural update introduced a separate `Clusterize.js` search view and isolated the `PinnedManager`, breaking the UI synchronization logic when agents are pinned from active search/fusion contexts, causing visual de-syncs and failed boundary tests due to numerical type coercion. | **Recovery:** Patched `RosterApp.js` to dynamically re-synchronize all `.pin-btn` DOM instances via `querySelectorAll(CSS.escape)` and automatically re-trigger active search filters upon pin events; enforced strict string coercion in `PinnedManager.js` initialization, resolving type mismatches and restoring a fully green test suite.
* **Regression:** Jest unit tests began parsing Playwright End-to-End tests resulting in a syntax error due to ES module imports outside of module scope, breaking the test suite build. | **Recovery:** Added `--testPathIgnorePatterns="playwright/"` to the `test:unit` script in `package.json` to properly isolate native Jest execution from the Playwright integration directory.
