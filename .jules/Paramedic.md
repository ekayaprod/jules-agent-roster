* **Regression:** A structural update introduced a separate `Clusterize.js` search view and isolated the `PinnedManager`, breaking the UI synchronization logic when agents are pinned from active search/fusion contexts, causing visual de-syncs and failed boundary tests due to numerical type coercion. | **Recovery:** Patched `RosterApp.js` to dynamically re-synchronize all `.pin-btn` DOM instances via `querySelectorAll(CSS.escape)` and automatically re-trigger active search filters upon pin events; enforced strict string coercion in `PinnedManager.js` initialization, resolving type mismatches and restoring a fully green test suite.
* **Regression:** A new Playwright end-to-end test directory was introduced which broke the Jest unit test runner by trying to parse non-standard ESM files. | **Recovery:** Ignored the playwright directory in the Jest configuration testPathIgnorePatterns to restore a clean test unit build state.
