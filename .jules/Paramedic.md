* **Regression:** A structural update introduced a separate `Clusterize.js` search view and isolated the `PinnedManager`, breaking the UI synchronization logic when agents are pinned from active search/fusion contexts. | **Recovery:** Patched `RosterApp.js` to dynamically re-synchronize all `.pin-btn` DOM instances and enforced strict string coercion in `PinnedManager.js`.
* **Regression:** Added playwright/ directory for UI testing, which jest-runtime attempted to parse during `test:unit`, triggering a fatal `Cannot use import statement outside a module` error. | **Recovery:** Configured `testPathIgnorePatterns` in `package.json` to explicitly exclude the `/playwright/` path, restoring compilation and achieving a clean test pass.