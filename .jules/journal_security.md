**Vulnerability:** The Fragmented Contract guarantees bugs. Magic strings and integers like `"success"`, `"error"`, `"loading"`, and `404` are scattered across the codebase, prone to typos and bypassing security validation checks. | **Prevention:** Extracted the magic values into `constants/ui.js` and defined strictly typed enums (`TOAST_TYPES`, `BUTTON_STATES`, `HTTP_STATUS`). Enforced immutability via `Object.freeze()` and deployed a test suite to ensure exact schema validation, eliminating the fragmented source of truth.
