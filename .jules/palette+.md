## Palette+ — [Reverse CRT Scroll Animation]
**Learning:** Reversing CSS background scrolling direction requires negating the target percentage in the `background-position` keyframe (e.g., from `100%` to `-100%`).
**Action:** When asked to reverse or change background animation direction, locate the `@keyframes` block and mathematically adjust the target `background-position` value.
