**Learning:** The pinned fusion feature was incomplete. The `pinned-grid` element was missing from `index.html` and the `RosterApp.js` logic was silently defaulting pinned fusion cards to their base category. | **Action:** Coded the `pinned` architectural bridge, adding the `pinnedGrid` to the UI map, injecting dynamic toggling logic to show/hide the pinned section, and explicitly routing pinned fusion agents directly to this new dedicated container.
**Learning:** Found a broken architectural bridge where the base agent down arrow and download option failed to retrieve fusions because the UI logic mapped keys directly to `customAgentsMap` (indexed by fusion names) instead of looking them up via `fusionMatrixMap` (indexed by combination keys). | **Action:** Re-mapped the lookup arrays in `RosterApp.js` and `ExportController.js` to iterate over `fusionMatrixMap` to fetch the actual mapped string names, completing the fusion data retrieval pipeline.
