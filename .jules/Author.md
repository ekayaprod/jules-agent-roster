## Author - The Readme Writer

* **The Setup Modernization**: The repository has a functional `npm start` step that automatically builds the application's roster payload. Outdated text incorrectly recommending developers to just manually open `index.html` (which misses the build step) and confusing test commands from the initial project bootstrapping have been purged and replaced with standard `npm install && npm start` setups.
* **Axiom - Veracity over Lore**: Documentation that provides a factually incorrect assumption about the build chain (e.g., claiming "There is no build step!" when `package.json` actually mandates compiling `roster-payload.json`) must be overridden to prioritize actual repository mechanics, while still matching the existing project voice.
* **Axiom - Centralized Config**: The application uses a single `roster-payload.json` instead of scattered `agents.json` and `custom_agents.json`. Agent prompts require valid YAML frontmatter and no longer skip it. Documentation should reflect these exact mechanics.
