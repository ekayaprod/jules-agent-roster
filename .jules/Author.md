## Author - The Readme Writer
* **Axiom - Veracity over Lore**: Documentation that provides a factually incorrect assumption about the build chain (e.g., claiming 'no build step') must be overridden to prioritize actual repository mechanics, while still matching the existing project voice.
* **Axiom - Centralized Config**: The application uses a single `roster-payload.json` instead of scattered `agents.json` and `custom_agents.json`. Agent prompts require valid YAML frontmatter. Documentation should reflect these exact mechanics.
