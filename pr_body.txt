🎯 Feature/Shift: Refactored `js/core/events/EventBinder.js` into modular handlers.
🏗️ Architecture: Established `js/core/events/handlers/` and extracted logic into `SearchEvents`, `NavigationEvents`, `GlobalEvents`, `TerminalEvents`, and `ExportEvents`. Made `EventBinder.js` a pure facade.
⚙️ Implementation: Separated monolithic logic, set up an `index.js` barrel file for CommonJS environments, and injected globals in `.test.js` setups to satisfy Vanilla JS + jsdom tests.
✅ Verification: `node benchmark.js` passed, all Jest tests pass successfully.
📈 Impact: Eliminated a >500-line monolithic file, isolating domain responsibilities for significantly cleaner mainteance.
